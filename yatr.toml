# Generated by yatr rollout — adjust as needed.

[tasks.dev]
run = ["npm run dev"]

[tasks.build]
run = ["npm run build"]

[tasks.check]
desc = "Astro type and content check"
run = ["npm run check"]

[tasks.favicon]
desc = "Redraw the favicon set from scripts/make-favicon.py"
run = ["python3 scripts/make-favicon.py"]

[tasks.ci]
desc = "CI pipeline"
depends = ["check", "build"]
